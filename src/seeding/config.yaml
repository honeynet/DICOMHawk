# Bait config baked into one instance per seed run.

honeytoken:
  honey_url: null      # base URL baked in as RetrieveURL (e.g. https://host)
  canary_pdf: null     # path to a PDF canary token baked in as EncapsulatedDocument
